# 变更记录

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.1.12] — 2026-09-24

**雨：现在真的按雨量下**

- **以前"下多少雨"只有半只脚踩在数据上。** 雨丝的密度、长度、速度只看 WMO 代码表
  （毛毛雨 0.18、大雨 0.85），而且粒子只在"天气代码或云量变了"时才重建——同一档
  雨下着下着从 1 mm/时 涨到 5 mm/时，一根雨丝都不会多。所以"毛毛雨显示得跟大雨
  差不多"是真的。
- 现在**降水量（mm/时）说了算**：`weather.precip_strength()` 把雨量过一遍对数刻度
  （跨数量级的东西线性看没意义），代码表退到"下限"的位置兜底；毛毛雨那一族封顶
  0.34，接口给的数再离谱也不会画成倾盆。粒子的重建 key 里也加上了雨量。
- **雨的画法整套重做**：雨丝分成远近两层（远的细/短/暗/慢，近的粗/长/亮/快），
  长度随雨量从毛毛雨的 3-8 像素拉到暴雨的几十像素，大雨另外蒙一层"雨幕"水汽；
  雨点砸在**窗外那条湿马路**上会溅开一圈圈小涟漪；雨丝按"粗细 + 透明度"分桶
  合并成一条路径再描边（一帧几十次调用，不是几百次）。
  顺手改掉一处物理错误：雨以前画在最后，会落在窗台和那盆植物上（看着像下在屋里），
  现在窗台、玻璃反光和盆栽都挡在雨前面。
- **雨量写在看得见的地方**：信息卡的"窗外"那行是"毛毛雨 19°C　云量 92% ·
  0.2 mm/时"，长卷悬停是"大雨 18°C · 云量 98% · 12.0 mm/时"，详情里也按雨量
  说"多大"（预览别的日子同样有——逐小时表里本来就有降水量）。那句"人话"跟着分档：
  毛毛雨是"细得几乎看不出雨丝"，暴雨是"雨点砸在玻璃上，路面已经湿得发亮"。
- `tools/snapshot.py` 多了个雨量参数，想对比"毛毛雨 vs 大雨"直接给数就行。

**桌面壁纸**

- **壁纸上的信息卡不再画"能点的东西"**：桌面上没有鼠标，可收起箭头、右下角刷新、
  每行右边那颗小箭头以前都画着（还占着位置），看着像能点。现在一概不画，右下角
  整块宽度都归那行小字——底部那句"天文 · 本地计算　天气更新于 14:15"能写全了。
- **长卷和它下面那行整点标签会被底部 dock 压住。** 壁纸是铺满整屏的，而常驻的
  底部面板压在最下面（本机 dash-to-dock 固定底部，就是 64 像素）：以前长卷下沿
  正好压着面板顶边、整点数字整个躲在面板里。现在按**真实的 workarea** 让开，
  长卷与标签整段钻出面板之外（读不到 workarea 就退回按像素估的余量）。
- 顺手修了两个"看不见但会咬人"的命中问题：空格收起信息卡之后，原来那些能点的
  方块还留在 `ui.info_rects` 里（点卡片原来那块空白处照样会跳去预览某个时刻）；
  长卷关掉之后 `ui.ribbon_rect` 也还留着（底下那条空白拖一下也会跳时间）。
  两处都在 `SkyPainter.draw()` 里按"没画就清空"处理。

**信息卡那条日弧**

- **鼠标映射改成按"一整天"算**：那条轨道铺的是 00:00–24:00（只有日出到日落那一段
  上了色），但处理器把整条轨道的宽度当成"日出到日落"来插值，于是鼠标在一天的区间
  上走、指到的是白天的区间（用户报的就是这个）。现在横坐标 → 一天里的比例 →
  那一刻，1/4 处就是 06:00 上下。
- 轨道上画出日出、日落两道刻度，日出的图标与时刻也挪到**日出真正落在轨道上的位置**
  （以前钉在左端，看着像"日出=00:00"）。

**两块"只有偶尔才咬人"的天气竞态**（2026-09-24 的 CI 把它逼出来了）

- 「换一扇窗」或按 `R` 正好撞上"上一次抓取还在飞"时，`refresh()` 直接返回
  False、**什么也不记**：换城市于是要等下一次十分钟周期才有天气（画面上写着
  "未联网"，可网络明明好好的），按 `R` 则只得到一句"稍等一下"就真的没有下文。
  现在这一枪会被记下来，等手上那枪回来立刻补上（连那句回声一起）。
- 手里那份还是**上一座城**的表时，不再按天补问：那一枪只带回薄薄一窗口
  （那天的前后两三天），而合并遇到"换了城市"是"旧的一律不算数"——整张表会被
  这一窗口顶掉，连"此刻"都没了。换城市本来就会强制整表刷一次，等它回来再补。
- **GUI 冒烟整轮不联网了**：以前只在"异步那一段"才把 `fetch` 换成假的，
  在那之前发出的真请求（启动那次、按 R 那次、换城市那次）会晚一步落地，
  把假数据随机盖掉——上面那条 CI 失败就是这么来的。现在从建窗口之前就换成
  假的，报告里带一个 `weather_net_calls`（应当恒为 0）。

（自动化测试 138 → 150 条：雨量刻度与文案、日弧的鼠标映射、壁纸卡片不带按钮、
长卷给 dock 让位、收起之后不留命中方块、换城市时的两条竞态，外加 GUI 冒烟里
的两条新探针。）

## [1.1.11] — 2026-09-24

**信息卡：点得动了、说得清了；桌面壁纸上的那张也能收成一条**

- **右上角那颗"收起 / 展开"以前点了没反应。** 处理器找的动作叫 `info_compact`，
  注册的却叫 `infocompact`——`lookup_action()` 找不到就静默返回（所以从 1.1.9
  起，C 键也是哑的）。名字现在只在 `chuang/actions.py` 里写一遍，键盘、卡片、
  托盘共用同一份；`tests/test_project.py` 新增一条静态检查盯着这类"名字对不上"。
- **"问一次真实天气"有回声了。** 右下角刷新（或 `R`）过去只弹一句"正在问…"
  就没了下文，网络不通还是接口变了都看不出来。现在拿到结果会再说一句：
  成功写清楚"天气更新于 14:12：中雨 18°C · 附近几天（09-24 ～ 09-30）也一起
  拿回来了"；断网就写"这次没问到（原因），还是 13:30 那份"；上一次还在问的
  时候再按，也会说明"上一次还没问完"。
- **卡片底下那行现在写"天气更新于几点"**（跟随真实天气时）。窗口窄就先让短句
  顶上来，不会挤到刷新按钮底下；数据来自别的日子、断网、没接入，写的都不一样。
- **壁纸上的那张卡不再是另一套版式**：菜单 → 桌面壁纸 → **壁纸上信息卡的版式**
  = 跟窗口一致（默认）/ 精简一条 / 完整版。以前窗口收成一条了，桌面上的还摊着。

**天气：预览到哪一天，哪一天就得是真的**

- 以前一次只要 2 天预报。把长卷拖到后天、或者"跳到某天某时"跳到下周，画面会拿
  窗口表尾（明天深夜）那一格当成那天的天气——**看着像预报，其实是另一天**。
  现在默认一次要 7 天；手上真没有哪一天，就照那一天单独问一次
  （Open-Meteo 的 `start_date`/`end_date`，16 天以内、过去 7 天以内），把结果
  **并**进同一张表：按天取并集，缺的那天就是缺，绝不拿邻天顶替。
- 真的没有（超出 16 天、没联网）时，卡片和详情会直说**"这天还没有预报"**，
  不编一个数出来。
- 云、天气现象、气温都按**那一刻**的逐小时值来（以前不管看哪一天，气温都显示
  "此刻"的读数，跳到三天后也还是现在这个度数）；体感 / 湿度 / 风只有"此刻"
  那一份，别的日子就不写了，详情里也说明为什么。
- 顺手修了两处数据问题：请求里漏了 `visibility`（于是详情里的"能见度"永远是
  默认的 24 km）和 `precipitation`；换了城市之后，缓存里上一座城的天气还可能
  被画出来（`Weather.matches()` 现在对不上就不画，等新数据回来）。
- "此刻"那一组读数不会被"补问三天后"顶掉：合并时只在**这一轮覆盖到今天**时更新。
  缓存里太老的日子会被丢掉，不会无限长大。

**键盘：窗口在最前面，按空格就有反应**

- 根因是**焦点跑到别的画布上**了：GTK4 的键盘事件只送到"当前有焦点的那个小部件
  所在的那块画布"，而菜单弹层一关，焦点会留在弹层里那颗已经不显示的小按钮上
  （另一块画布）——空格、左右键就此没有回声，看起来就是"窗口明明在最前面，
  按空格没反应"。现在窗口重新拿到焦点、鼠标回到窗里、点了画、菜单合上，这四个
  时刻都把焦点交回画面（新模块 `chuang/keys.py`）。
- 键盘那一摊（空格 / `C` / `R` / 左右 / `Home` / `Esc` + 焦点回声）从 `app.py`
  拆到 `keys.py`；GUI 冒烟新增探针：把这几颗键按一遍、**点一次卡片右上角**、
  以及"菜单关掉之后焦点必须回到画面上"。
- 跳到别的日子时，那句提示会带上日期（"正在看 10-03 18:40 的窗外"）。

**其它**

- 长卷悬停的注脚也跟着用**那一刻**的气温（以前整条都写着"此刻"的温度）。
- 测试 120 → 138 条：天气的逐日覆盖与合并、信息卡那行"更新于"、壁纸版式三档、
  动作名注册检查，都进了自动化。
- **CI 那次 180 秒超时**是冒烟测试自己的毛病，已经拆掉：以前为了等后台抓取
  落地，在探针回调里**嵌套跑主循环**（`MainContext.iteration()`），还在无窗口
  管理器的 Xvfb 里真的去开菜单弹层（要抢键盘 grab）——这两样在某些环境里会把
  整条测试挂住。现在那几条改成 `GLib.timeout_add` 一步排一步（不需要嵌套循环、
  不开弹层），并且超时时会把**已经收到的输出**带进失败信息里，下次一眼能看出
  卡在哪一步。顺带把"菜单关掉后收焦点"这一下排到 idle 里做（等弹层拆完再动），
  免得跟 GTK 拆弹层的过程抢手。

## [1.1.10] — 2026-09-23

**修掉「安装更新」按钮：它从 1.1.8 起就点不动**

现象：在 1.1.8 里点「下载并安装 1.1.9」，等一会儿弹出一句
「下载 v1.1.9 的安装包失败：'UpdateController' object has no attribute '_verify_deb'」，
到哪一步都不对劲——**包其实已经下好了**，只是被报成了"下载失败"。

- 根因是个名字：1.1.8 加了"装之前先校验 SHA256SUMS"这道关，调用处写的是
  `self._verify_deb(rel, target)`，而方法其实叫 `verify_deb`（**多打了一个下划线**）。
  于是下载完成后、校验那一步抛 `AttributeError`，又被外层 `except` 兜成了
  "下载失败"。两条路（「下载并安装」「只下载 .deb 安装包」）都是这么坏的。
- 单测当时只测了 `verify_deb()` 本身（它一直是对的），没人真的去调一遍那条
  后台线程——所以整条流水线绿着，按钮却是坏的。
- 现在：调用改回 `verify_deb`；**下载与校验分开兜**——校验自己出错时，包已经躺在
  磁盘上了，就按"下了但没验过"报，并把文件路径和手动安装的命令一起给出来，
  不再骗人去重下。「只下载」那条路也补上了创建下载目录（之前目录不存在就直接失败）。

**这次还顺手补了两道网，专治这类"跑起来才知道"的错**

- `tests/test_install.py` 现在真的跑一遍后台线程（用 `file://` 当下载地址，不联网）：
  对一个好包要走到"交给安装器"、对一个被改过的包要拦下、对一个下不到的地址要
  报"下载失败"。「只下载」那条路也走一遍。**把调用改回 `self._verify_deb` 再跑，
  这组测试当场就红**（试过）。
- `tests/test_project.py` 新增一条静态检查：**类里 `self.xxx(...)` 调的每个方法，
  必须在同一个类里有定义**。拿它去查装着的 1.1.8，它准确地指出
  `UpdateController.self._verify_deb() 在第 149 行 / 第 199 行`。

## [1.1.9] — 2026-09-23

**看着更顺，也看得更明白：帧率可以调了，左上角那张卡能点了**

**帧率：默认从 10-14 帧提到 60 帧，而且按机器可挑**

- 以前重绘写死在心跳里（有降水 0.07 秒一次、安静时 0.10 秒一次），窗口不在
  前台时还要再 ×5——也就是说，那扇摆在桌面上、拿不到焦点的窗，实际只有
  **2-3 帧/秒**。云在飘、雨是斜的、街上的人和车都在走，这个帧率看着就是一个字：顿。
- 现在重绘由 GTK 的**帧时钟**驱动（`chuang/frames.py`），跟着显示器的刷新走：
  菜单 → 看 → **画面流畅度** 可以选 24 / 30 / 45 / 60（默认）/ 120 帧每秒，
  写进配置、重启也记得。窗口没拿到焦点时最多按 30 帧跑（省电）——这条会明说，
  不偷偷降级。帧时钟万一停摆（某些远程桌面会这样），心跳里还有一条每秒一次的兜底。
- 顺手把信息卡整张离屏缓存：它一帧要下上百笔，而大多数帧纹丝不动。
  1280×800 的雨天一帧 19.7 → 15.6 ms；本机 960×620 一帧 10-11 ms、
  1920×1080 一帧 21-24 ms（README 里那张表已按实测更新）。

**「此刻的事实」不再是"一列文本"**

- 每一行前面有了自己的**矢量图标**：太阳、日出/日落（地平线 + 半个太阳 +
  朝上/朝下的箭头）、月相（按真实相位画明暗交界线，新月也留一圈轮廓，不会"看不见"）、
  云/雨/雪/雾、风、刷新。全部是几笔 Cairo 画出来的，不引图片、不引图标字体——
  依赖表一个字节都没变。
- 排版重做：城市 + 「此刻 / 预览」小标、大字时刻，然后是**日出到日落的"日弧"**
  （此刻在哪儿、还剩多少天光），再往下才是"图标 + 标题 + 数值"的一行行事实，
  副值（金色时刻、月出月落、云量、湿度）跟在数值后面——小窗口里装不下就整条
  不画，不会挤成半句话。
- **这张卡现在可以点**：点日出 / 日落 / 月亮 / 太阳跳到那一刻去看（`Esc` 回来）；
  点「窗外 / 风」摊开完整天气（体感温度、阵风、能见度、数据来源与更新时间）；
  右上角的箭头把卡片收成一条（只留时间和那句话）；右下角的刷新按钮立刻重问
  一次真实天气；日弧上悬停看时刻、点一下跳过去。鼠标停在哪一行，整行会亮起来，
  光标也变成手型。
- 「显示此刻的事实」与「精简模式」现在都记得住：以前用空格关掉卡片，重启之后
  它又自己回来了。

**顺手做的几处**

- **修掉一个只有 CI 才看得见的时区 bug**（新增测试当场抓到的）：`SkyEngine`
  的出厂值恰好就是西安 + `Asia/Shanghai`，而 `set_location()` 在"参数与当前值
  完全相同"时会提前返回——于是**时区根本没被解析**，`local_now()` 悄悄退回
  **系统时区**。本机系统时区也是 +08:00，所以两年都没露馅；CI 跑在 UTC 上，
  日出（06:32）就变成了"前一天 22:32"，那一天于是成了"日落早于日出"，
  日弧直接画不出来。现在引擎一建出来就把时区解好，`set_location` 也不再提前
  返回；GUI 冒烟里加了一条硬核对（`_now()` 的偏移必须是 +08:00），
  并且以后 `TZ=UTC` 跑一遍全量测试就能盖住这类"本地看不出来"的坑。
- 今日天色长卷：日出 / 日落不再是两道刻度线，而是带图标与时刻的标记；悬停时
  那个小气泡里多一行"这一刻的天气"（晴雨 + 气温 + 云量）。
- 提示条（toast）左边多了一枚按内容给的图标（信息 / 警告 / 刷新），点一下就收掉；
  带详情的仍然点开看。
- 换城市的列表里，每个候选都显示**当地时刻**和"比这里快 / 慢几小时"，
  正在看的那个城市带「正在看」标；鼠标停上去还能看到经纬度。
- 菜单：看 ▸ 里新增「信息卡精简模式（C）」「显示今日天色长卷」「画面流畅度」。
- 新增快捷键：`C` 收起 / 展开信息卡，`R` 立刻刷新天气。
- 拆出 `chuang/infocard.py`（那张卡的交互）与 `chuang/frames.py`（帧率与帧时钟），
  `app.py` 仍在 1100 行以内；自动化测试从 92 条加到 **114 条**
  （新增：信息卡的行与图标、缓存 key 不许漏参数、图标不许画出自己的方框、
  卡片缓存真的命中、帧率档位与失焦降档、坏配置的纠正；GUI 冒烟里也真的点了一遍那张卡）。

## [1.1.8] — 2026-09-23

**这一版修的是"说过的话没做到"**

一次完整的代码审阅（`CODE_REVIEW.md`）把项目从头到尾读了一遍，连同只读实测。
结论里最要紧的不是"少了什么功能"，而是几处**行为与承诺不一致**：
关掉的开关还在生效、号称算得准的月亮其实偏了几度、把时间当作卖点的东西
在日出日落上差了四分钟。这一版逐条修掉，并且给它们配上了自动化测试。

**月亮的位置一直是错的（最严重的一条）**

- Meeus 第 47 章那两张周期项表（47.A / 47.B）的**角度系数写错了**：第一项
  `6288774·sin(M′)` 的 M′ 被写成 0，六度多的主项直接消失，于是月亮的黄经
  整体偏 **5°**、距离偏 **3 万公里**（曾按 36.2 万公里画，实际是 39.5 万公里）。
  现在两张表都换成原表的全 60 项，并逐点对照过：与独立历表（pyephem，走 JPL
  口径）比，地心黄经残差 < 1′、距离残差 < 10 km。
- 月亮还缺一道**地平视差**修正。它只有 38 万公里远，地心坐标与地面看到的
  位置最多差 **61′**（约两个月面直径）——于是月亮会"还没升起来就先出现在窗里"。
  现在按 Meeus 的口径把视差减掉，正上方不修、贴地平时修满。
- 顺带把画面里"亮面朝哪"的那道计算也统一到同一套坐标上。

**日出日落差了四分钟，月中天能差两个小时**

- 升落的判据（−0.833°、±6°、−12°、−18°）说的都是**几何高度**，
  而代码拿的是"含大气折射的高度"去比——等于把折射算了两遍：日出早 4 分钟、
  日落晚 4 分钟。现在太阳的升落、金色时刻、暮色一律用几何高度判。
- 月出月落同样换了口径（h0 = 0.7275π − 34′，π 随距离变），
  以前还会跟着折射一起漂。
- 现在的成绩：四座城市、三个季度、五个时刻（日出/日落/中天/月出/月落）
  与**美国海军天文台（USNO）**的官方数据全部相差 **0.5 分钟以内**。
  这几组数字现在是测试用例，改坏了会当场变红。
- `solar_noon()` 原来用"把方位角迭代到 180°"的定点迭代，太阳接近天顶时
  会直接跑飞（西安 2026-06-21 差 **2 小时 46 分**）。改成对时角求零点。

**开关终于可信了**

- **「跟随真实天气」关掉之后，画面里还有天气**：这个开关以前只挡住了联网刷新，
  磁盘缓存（第一次抓取成功后就一直躺在那儿）照样被画进窗里，重启也一样——
  开关看起来是坏的。现在关掉就是真的关掉（`WeatherService.effective`），
  长卷与壁纸也一起停。
- 关掉天气的人，信息卡不再写「未联网 · 仅天文模式」（那会把人指去查网络），
  改成「你关掉了天气 · 只看天」。
- **双击托盘图标会把窗口叫到前面了**：托盘调用那个处理器时一个参数都没传，
  而它要求一个必填参数，于是每次都抛 `TypeError`——D-Bus 方法处理器抛异常
  意味着这次调用永远等不到回复。顺带把那句"托盘图标点一下就能再打开"
  改成菜单里真实存在的路径。

**装更新这件事，现在有据可查**

- 自动更新会把 `.deb` 下下来、再交给 `sudo` 装。这件事以前**不校验**任何东西：
  CI 早就把 `SHA256SUMS` 挂在 Release 上了，客户端却没用。现在下载完先算摘要
  比对，对不上就拒绝安装并把两个摘要都摆出来；**没有校验文件也不自动装**，
  只把包留在那儿让你自己决定。远端文件名也先过一遍 basename。

**桌面状态与缓存**

- 接管壁纸时会把 `picture-options` 改成 `zoom`，而「还原成原来的壁纸」以前
  只还原图片、不还原缩放方式——用拉伸/居中的用户被永久改掉了设置。现在
  一起记、一起还。
- 换城市之后可能还挂着**上一座城市的天际线**：城市那块位图的缓存键里没有
  城市种子，光没变就不会重画。现在键里带上种子，另加 `invalidate_location()`
  在换城市时把楼群数据与所有离屏缓存一次清干净（壁纸线程也走同一条）。
- 生成「离线动态壁纸」时不再"先删旧帧再生成"：改成画进另一本相册、画完再
  切过去。中途失败最多是这次没换成，不会留下一个指向已删文件的空桌面。
- 手输经纬度不再按经度取整时区（柏林的夏天会差整整一小时）：改成联网问一次
  IANA 时区，断网才退回取整，并且在提示里说明可能差一小时。

**工程上补的那点东西**

- **先说个事故**：这一版第一次 push 到 master 之后，CI 的「单元测试」红了——
  测试依赖里漏了 `python3-gi-cairo`，`PangoCairo.create_layout()` 抛
  `KeyError: 'could not find foreign type Context'`，离屏出图全挂。
  因为 `build`/`publish` 都挂在测试后面，所以那次 push **没有打出 v1.1.8**，
  Release 里什么都没有（这也正是"跑不过就不打包、不发版"这条线该起的作用）。
  补上依赖；现在 `tests/test_snapshot.py` 会专门检查这块胶水在不在，
  缺了就给出一句人话的失败信息，而不是一个看不懂的 KeyError。
- **`app.py` 拆了**：它曾经 2000 行、一个类同时当菜单表、壁纸调度器、更新安装器、
  诊断器和窗口用。现在按天然边界分成六块——
  `actions.py`（菜单与动作只有一处定义）、`wallpaper_ctl.py`（桌面壁纸那一摊）、
  `update_ui.py`（下载/校验/安装/重启）、`dialogs.py`（自绘的小窗口）、
  `diagnostics.py`（诊断文本，纯函数）、`app.py`（窗口本身，1000 行）。
  行为一点没变，但"改菜单"和"改心跳"再也不会出现在同一个文件里。
- `render.py` 里那五张离屏图（天空、云、城市、暗角、窗台）不再各自手写一遍
  "key 对不对、尺寸对不对"，统一收进一个二十行的 `CacheSlot`；`invalidate_location()`
  也就成了"把这五个槽一次清掉"。（这次重构是用"冻结时间逐像素对照"验的：
  四个场景重构前后**一个像素都没变**。）
- 有了 `tests/`：92 条测试，覆盖"天文对 USNO / 月亮表与视差 / 中天 / 天气开关 /
  壁纸槽位与帧目录 / 安装包摘要 / 配置纠正 / 诊断文本 / 离屏绘制冒烟"。
  只用标准库的 `unittest`，仍然零第三方依赖；`make test` 或 `make check` 一条命令跑完，
  CI 每次 push 都跑一遍，**跑不过就不打包、不发版**。
- 其中有一条是**真的把窗开起来**跑的（`tests/gui_smoke.py`）：自己起 Xvfb、自己换
  `HOME` 与 `APP_ID`，把菜单里出现的每个动作逐个核对注册情况、把 9 个勾选项与 2 个
  单选项的参数类型/状态类型逐个验一遍、把能安全激活的动作真的激活一次、把 5 个
  对话框各建一次、再走一遍"关掉天气""换城市"。两张清单（能安全激活 / 为什么不能激活）
  是强制分类的——**新加一个动作，必须想清楚它会不会动用户的桌面**，否则测试会红。
- 还有一组"项目规矩"测试（`tests/test_project.py`）：版本号只准出现在一处、
  不许用这台机器上没有的 libadwaita 控件、不许用会静默失败的 `activate_action`、
  不许引入第三方库、`subprocess.run` 必须有超时、`Popen` 必须把输出丢掉。
  这些以前只是 AGENTS.md 里的几行字，现在违规就会红。
- 配置里多了 `schema` 版本号（老配置文件读进来会升到当前版本），顺手修了一个
  `XDG_CONFIG_HOME` 被设成空串时配置会写到"当前工作目录"的坑。
- `wallpaper.py` 顶部那段"必须在 a/b 两个文件名之间交替写"的注释是错的
  （1.1.4 就改成就地更新了），一并改掉；`DESIGN.md` / `README.md` / `AGENTS.md`
  里同类说法也统一。机制类结论以后只写在 `DESIGN.md` 一处，代码里引用它。
- `chuang --help` 现在真的有输出，认不出的参数会报错而不是被静默忽略
  （`chuang --citty` 以前会当成正常启动）。

## [1.1.7] — 2026-09-23

**发版这件事，现在只剩 push**

以前每次发新版都要手动打 tag、手动把 `.deb` 拖到 Release 上，一步忘了就少一个包。
这一版把整条链路交给 CI，另附一个在本机就能跑的脚本。

**push 就发版**

- 工作流 `.github/workflows/release.yml` 现在也盯着 **master 上的提交**：只要
  `chuang/__init__.py` 里的版本号比现有 tag 新，就自动打 tag（`v1.1.7`）、打包 `.deb`、
  建 Release 并把它挂上去，Release 说明直接取自本文件里对应版本的段落。
- **普通提交不会误发版**：版本号没涨、或这个 tag 早就发过时，CI 只打包自检、把 `.deb`
  留在本次运行的 Artifacts 里，不碰 Release。临时把版本号改小来测「检查更新」的界面，
  不会意外发出去。
- 推 tag、以及在 Actions 页面手动运行，仍然都能发版；手动那条用来补传 `.deb` 或重跑
  失败的构建。
- 打包后多两道自检：`.deb` 里的版本号必须与 `chuang/__init__.py` 一致、包不能是空的。
  Release 上除 `.deb` 外还多一份 `SHA256SUMS`。

**本机也能一条命令发版：`./packaging/release.sh`**

- 默认走 CI：先校验版本号比现有 tag 新、`CHANGELOG.md` 里有对应段落、工作树干净、
  本地已推送到 `origin`，然后打 tag 推上去、等 CI 跑完，最后把 Release 上的 `.deb`
  下载回来验一遍版本号与包结构。
- `--local` 不走 CI：本地打包，用 GitHub CLI（`gh`）直接建 Release 并上传，适合补传或换包。
- `--dry-run` 只做检查，什么都不改。`make release` 是它的快捷方式。

## [1.1.6] — 2026-09-23

**街上：树、车流、人的数量，以及"空格"**

这一版全在街上——修掉几处画错的地方，补上路边该有的树，让车流和人流真的跟着
时间、星期与天气变。

**修掉的两处车的问题**

- **车窗横向错位**：车窗那一圈点原先只按"车身高度的比例"算，没有跟着每种车的
  实际车顶线走——SUV 和轿车的玻璃会从车头/车顶冒出去一块。现在把车窗、车门缝
  一律裁进车身路径里（`stroke_preserve` 之后 `clip`），怎么算都不会跑到车外。
- **车轮外面套着一个黑圈**：轮拱里那点暗原先画的是**整个圆盘**，而圆的直径比
  门槛线还低，于是在车身下面露出一圈黑边，像"车轱辘外面又描了个圈"。现在暗腔
  只在门槛线之上画（轮拱本来就是车身挖出来的洞），露在外面的只有轮胎本身。
  顺带把轮拱收小一点、车灯与后视镜也收小一点（以前那两块白色小方块太抢眼）。

**路边种上了行道树**

- 站在**远侧人行道**上，比车道更远，所以按透视只能比人矮一档地画：整棵约
  0.05 个窗高（旁边的楼是它的好几倍高），树冠刚好到楼的两三层，不挡城市、
  也不抢街上的人。
- 每棵都有树干、几团叠出来的树冠、树冠下缘的暗、太阳那一侧的亮边，以及**和
  人车同源的影子**（太阳低就长、阴天下雨就没有）；有风时树冠会轻轻地摆。
- 位置由城市种子决定，疏密不均，不会排成等距的栅栏。

**路上的车流与人流，跟着时刻 × 星期 × 天气变**

- 新增 `street.activity()`：以本地时刻插值出"车流"和"人流"两条曲线，再乘上天气。
  工作日两个高峰（8 点、18 点）车最多、行人也最多；周末高峰晚一点、平一点；
  凌晨 2-5 点几乎没人；雨天行人明显变少、骑车几乎消失，雪天更少，起雾时大家都
  少一些，打雷时行人大减。实测（12 辆车＋2 辆公交＋12 个行人＋4 辆自行车的班底）：
  03:00 约 1 车 0 人；08:00 约 11 车 11 人；13:00 约 6 车 7 人；18:00 约 12 车 11 人；
  23:00 约 4 车 2 人。
- **"少几辆"而不是"每辆都变淡"**：每辆车一趟一趟地过画面，每过一趟就是"第几趟"；
  按当时的密度决定这一群里放几辆上路，具体哪几辆逐趟轮换（均匀挑选，不是永远
  挑前几个）。换班发生在车正好绕回起点的那一刻——画面外，看不见谁凭空出现或消失。
- **不再有两辆车叠在一起**：同一条车道上的车速度完全一致、发车时间均匀错开
  （相位差被速度差吃掉，几分钟后就会叠在一起，这一版把它们摁住了）；公交插在
  两辆车中间、跟这条道同速。行人、骑车人也按方向均匀排开。
- 预览（时间旅行）时，天色停在预览那一刻，**车流密度也跟着预览的时刻走**——
  拖到凌晨三点，街上就该是空的。

**空格：显示 / 隐藏「此刻的事实」**

- 这个功能一直都在，坏的是"按下去没反应"：键盘焦点默认可能停在标题栏那颗
  「图钉」或菜单按钮上，空格被 GTK 当成"激活这个按钮"吃掉了。现在开窗时把焦点
  交给画面本身，并且**标题栏按钮点完不再抢焦点**（`focus-on-click=False`），
  窗口捕获阶段的键盘控制器照旧兜底。
- 顺带修掉一处不同步：用空格关掉信息卡之后，菜单/托盘里「显示此刻的事实」的勾
  还挂着——现在会一起更新。
- 记一笔踩坑：GTK 4.6 上 `Gtk.ShortcutController` 的 MANAGED / GLOBAL 作用域
  对送到窗口的按键**不触发**（实测只有 LOCAL 会），所以别指望用它来救快捷键。

**「今日天色」长卷往上挪：别再被底部面板挡住**

- 很多 Linux 桌面把面板或 dock 放在屏幕底部（Plank、Cairo-Dock、扩展版
  Dash to Dock、KDE 面板……），长卷原先画在 94% 高度上，正好被压住。
  现在按**像素**倒推留白（面板高度是"多少像素"而不是"百分之几"）：
  `render.scene_height()` 让长卷下沿距窗口底边约 65px（实测各尺寸下 63-75px），
  景色按这个略矮的高度排版，最下面那条留给窗台继续铺下去——窗外那幅画面
  一点没少，只是整体上移了一点，比例保持不变。
- 长卷的点击/拖动热区跟着它一起走（`ui.ribbon_rect` 本来就是窗口坐标）。

## [1.1.5] — 2026-09-23

**把窗外的世界重画了一遍：城市、车、人、盆栽，以及它们该有的影子**

这一版只有一件事：让窗外"像个地方"，而不是几块剪影。天没动，动的全是地平线上下。

**城市（`chuang/city.py`，新文件）**

- 分成三层：远山（一条起伏的山脊）→ 远景楼群（被空气洗淡的另一圈楼，夜里星星点点
  几盏灯）→ 近景建筑。
- 楼不再是一模一样的矩形：塔楼（层层收进）、写字楼、住宅（阳台带）、坡屋顶小楼、
  带雨棚的店面、锯齿屋顶的厂房；屋顶上还有水箱、楼梯间、空调机组、烟囱、天线
  （高塔的天线夜里会闪一盏小红灯）。
- 颜色不再是纯黑：给每栋楼抽一个固有色（水泥灰 / 米黄 / 青灰 / 灰蓝 / 砖红 / 陶土 /
  深蓝 / 雪白）与明暗；白天受天光、夜里退回"城市光晕 + 一点点月光"，永远留得住颜色。
- **受光面与影子**：太阳偏左，楼的左侧就有一条受光面、影子往右倒；太阳压得低，
  受光的那条边会亮成一条线；太阳在正前方（逆光）时正面是暗的；太阳落到地平线下就
  退回剪影。云厚、下雨、起雾时直射光被削弱，影子跟着淡掉——「阴天没有影子」这句
  人话，现在画面上也真的是这样。
- 楼与楼之间互相遮挡（背光侧压在邻楼上），楼根有环境光遮蔽，玻璃幕墙会映天、带
  一条斜的高光，普通楼的窗格白天是映天的深色玻璃（有的拉了百叶），夜里按比例亮灯。

**街（`chuang/street.py`）**

- 车轮不再"骑在圆圈上"：轮拱里的暗腔 + 轮胎 + 轮毂先画，车身最后压上去、底边绕过
  轮子挖出两个轮拱，只有贴着地面的那一段轮胎露出来。
- 车型有轿车 / 掀背 / SUV / 厢式 / 出租 / 公交，各自有自己的侧面轮廓、车窗形状、
  车灯位置与配色（银、白、黑、深蓝、红、墨绿、出租车的黄）。车头朝着行驶方向。
- 白天车窗映天、有车门缝与后视镜；夜里车灯在地上洒一片光、尾灯是红的，雨天车尾有
  一团水雾。
- 行人会走路了：髋—膝—踝的两条腿交替、手臂反相摆、有肩有躯干有头（还有头发），
  雨天撑伞，另外有背包的、小孩、牵着狗的人。走路节奏按"每秒走多少身位"算，
  车快人慢，各走各的。
- **人和车都投影**：方向与长短由太阳方位角、高度角决定（太阳越低影子越长），
  下雨/阴天自动消失；车底与脚下另有一片贴地的软影。
- 人行道两侧各有一条路缘、路面上有淡淡的车道线，夜里路灯会在地上洒光池。

**地面与窗台**

- 地平线到窗台之间拆成"远侧人行道 → 马路 → 近侧人行道"，路面按天光着色、太阳那一
  侧更暖，建筑把影子投在路面上（太阳低就长）。
- 窗台多了一条**窗框下框**：街与窗台之间终于有了一道横向的"窗里/窗外"分界；
  台面加了石纹与石缝，不再是一整片平色。

**那盆植物**

- 从"多肉莲座"换成真的盆栽：上宽下窄的陶盆 + 一圈盆沿（沿下有暗、沿上有高光）+ 露
  出来的土，几根枝干上长了十几片大小朝向各异的叶子（有的偏黄绿、有的偏蓝绿，
  叶片有明暗渐变与叶脉）；太阳那一侧更亮。它的影子照旧按真实方位角与高度角投在
  窗台上，这一版把影子调得更实在了。

**性能**

- 楼多了几十倍细节，但城市与窗台各自缓存成一张离屏图（只随光照量化档位重画），
  窗格也按"样式 + 亮度档"攒成整条路径一次填充：1200×760 下一帧白天 5.7 → 7.8 ms、
  夜里 9.0 → 9.8 ms（实测），40 fps 的动画不至于因为这一版变卡。

**顺手修掉的小问题**

- `Scene.period_name`（信息卡上那句"白天／金色时刻／…"）里有一句永远走不到的
  分支：`return "金色时刻" if a > -0.9 else "日落"`——外层已经保证 `a > -0.9`，
  "日落"是死代码。现在按太阳高度分段：>6° 斜阳、>0° 金色时刻、> -2.5° 日落
  （-0.833° 是太阳含折射压在地平线的高度）……顺带把 4°-6° 那一段的字与
  应用自己的"金色时刻结束 = 6°"对齐了。
- `scene.skyline_layers()` 生成的"楼"里带了一堆只给旧画法用的字段（`grid` / `rnd` /
  预生成的窗格数），新画法按像素算窗格，这些字段一并删掉。
- 旧的 `_colors()` 返回两个颜色、只被街道当基色用，现在收成一个。
- `render.py` 里几处早已没人用的变量与 import（`colour`、`sh`、`mu`、
  `import astronomy as A`）一并清掉（`pyflakes` 干净）。

## [1.1.4] — 2026-09-23

**"桌面显示的是这个文件上一版"——真凶找到了**

现象（用户先观察到的）：sky-a / sky-b 两张都在正常更新，但桌面显示的却是
"这个文件名上一个、上几个时候的版本"；关掉「壁纸上显示此刻的事实」之后，
带信息卡的旧图还会和新图交替出现三五轮。

读了 gnome-shell 的代码（`/org/gnome/shell/ui/background.js`，从
`libgnome-shell.so` 的 gresource 里取出来看），并在本机复现，机制是这样的：

- gnome-shell 把壁纸的**解码结果按文件缓存**（`Meta.BackgroundImageCache`），
  而且**只为"当前正在显示的那个文件"挂文件监听**：只有它收到"这个文件变了"，
  才会 `purge` 掉缓存并重读。
- 老做法是"写另一张 → 把壁纸 URI 切过去"。切过去的瞬间，shell 是从**缓存**里取图的：
  这个文件上一次的解码结果（最多 20 秒前的画面，时间自然是旧的）。
  能不能刷新，取决于"文件变化的通知"和"设置变化"哪个先被 shell 主循环处理到——
  于是就成了"有时候"。

修法：

- 常态**就地更新桌面正在显示的那个文件**（它正是被监听的那个）：内容一变，
  shell 自己 purge + 重读，画面直接换成新的，永远不会再出现"上一版"。
- 只有"接管"（桌面挂着别人的图）或每 5 分钟兜底一次，才真的换一次文件名；
  换完连碰两次 mtime，逼 shell 丢掉旧解码。
- 「壁纸诊断」里多了一行"上次真正换过文件名（URI）是多久以前"。

**顺带更正一条写错的旧结论**：README / AGENTS 里曾写"GNOME 不会因为同一路径的
文件变化而重载壁纸，必须在 a/b 之间交替写"——**这条是错的**（至少在 GNOME 42 上）。
本机实测：同一路径内容变化会正常重载，反倒"换文件名"才会让桌面拿到旧的解码结果。

## [1.1.3] — 2026-09-23

**修掉"时间有时候会定格"的真正来源**

- 现场排查发现：进程只有一个、壁纸文件每 10 秒照常刷新、桌面显示的也确实是最新那张
  （截图与文件逐像素比对，平均差 <1/255）。所以问题不在"换图"，而在**心跳**。
- PyGObject 里，`GLib.timeout_add` 的回调**只要抛过一次异常，GLib 就会把这个定时器
  整个移除**：进程还在、托盘还在、菜单还能点，但窗口与壁纸永远停在那一刻——这正是
  "有时候左上角的时间不对"最难查的那种。心跳现在整段包了 try，出错也只记一笔，
  绝不让定时器死掉。
- 同理，`gsettings` 调用以前**没有超时**：dconf 一旦卡住，GTK 主循环会被一起拖死
  （窗口不响应、壁纸定格）。现在所有 gsettings 调用都带 5 秒超时。

**新增「壁纸诊断…」（桌面壁纸 ▸ 里）**

- 这类"有时候"的毛病，光靠转述很难查。现在出问题的当下点一下，就会弹出可整段复制
  的一份报告：本进程 pid 与单实例锁、系统里其它「窗」进程、代码版本 vs dpkg 装的版本、
  壁纸跟随开关与间隔、桌面此刻挂着哪张、那张是几秒前写的、是否需要顶上另一张、
  最近一次换图成功与否、心跳兜住过几次异常。

## [1.1.2] — 2026-09-23

**发现新版本可以直接装好了**

- 菜单里点「下载并安装 vX.Y.Z」：程序自己下载 `.deb`，然后开一个终端跑
  `sudo apt-get install`——你在终端里输一次密码就能装完，过程、报错都看得见。
  装好后它会发现版本变了，问你「现在重启吗」；选「现在重启」会先等旧进程退出
  （单实例锁还握着），再拉起新版本，壁纸最多停一两秒。
- 保留「只下载 .deb 安装包」这条路（下载到「下载」目录，并给出可复制的安装命令）。
- 没有终端可用时，会把安装包路径和命令原样给你复制，不再只说一句"下载好了"。

**提示里的长内容可以复制了**

- toast（屏幕下方那条提示）只能看不能抄。现在凡是带路径 / 命令 / 报错的提示，
  末尾会多一个「详情」：点一下弹出可选中、可一键复制的窗口。
  下载结果、安装失败原因、换壁纸失败都改走这条路。

**预览（时间旅行）更像"活的"**

- 之前拖到别的时刻，天色和云都在动，街上的人车却冻住了。现在以预览那一刻为
  起点继续走：天色停在你选的时刻，人车照常在街上走。窗口和壁纸的说明也一起改了。
- 行人的速度以前几乎和汽车一样快（看上去像在赶车）。现在按真实观感拉开档次：
  汽车 9–11 秒穿过画面、公交 15 秒、自行车 21–26 秒、行人 54–83 秒。
- 「看 ▸」里新增 **跳到某天某时…**：日历 + 时/分选框一步到位，不用来回拖长卷
  （原来的拖动、滚轮、左右方向键都照旧）。

**修掉"壁纸在刷新，左上角时间却没变"**

- 左上角那行时间就是画面里的「此刻的事实」卡片，它一直跟着时刻重画（做过逐像素
  比对验证）。真正的问题是**桌面偶尔没接受新图**：`gsettings` 写进去了但当前生效
  的那个键（深色模式是 `picture-uri-dark`）没变，于是文件在更新、桌面还挂着上一张。
- 现在每次换图都会**回读校验真正生效的那个键**，没变就重设一次再验；仍然不行会明确报错。
- 另加一道兜底自查（每 5 秒）：发现桌面挂着的那张比另一张还旧，立刻把新的顶上去，
  并马上重画一张最新的。开机时也能靠它把"上次关机那张"瞬间换掉。
- 顺手修掉：窗口收进托盘时标题栏的时间不会更新，重新打开时会停在旧的一分钟上。

## [1.1.1] — 2026-09-23

**修好「开机自启」**

- 老版本把自启项写成写死的那一刻的路径，换了安装方式（源码安装 → `.deb`）之后
  它就指向一条已经不存在的命令。GNOME 的 `systemd-xdg-autostart-generator`
  只会静默忽略这条自启项（日志里才有一句 `Exec binary ... does not exist`），
  于是开机后什么都没发生，桌面还挂着"上次关机那一刻"的壁纸。
  现在每次启动都会核对并就地修好这条自启项，命令行也从"当前这份安装"里现取。
- 新增「开机时直接进托盘（不弹窗）」：只想让壁纸跟着走的人，不必每次都看见窗户。
- 自启时立刻重画一次壁纸（以前要等 2.5 秒，且只尝试一次）。

**修好壁纸「两张来回闪」**

- 写到哪个文件，不再相信配置里记的槽位，而是先看**桌面此刻真正显示的是哪一张**
  （GNOME 深色模式用的是 `picture-uri-dark`，以前只看 `picture-uri` 会判断错）。
  这样每次换图 URI 一定变化，GNOME 一定重新读文件，不会留下一张过期的旧图。
- 加了进程间文件锁：同一时刻只有一个「窗」能写壁纸。第二次启动会把已有窗口叫到
  前台然后退场，不再出现"两个写入方各写一个文件、桌面来回闪"的情况。
- 换城市后立刻重画壁纸，不再让桌面朝着旧城市待满一个周期。
- 从挂起醒来、系统时间被改动时，画面与壁纸都会立刻追上当前时刻。

**其它**

- 壁纸跟随的节奏可选：10 秒 / 30 秒 / 1 分钟（默认仍是 10 秒）。
- 「还原成原来的壁纸」不再可能还原成一张过期的天空：早期版本会把"我们自己画的
  天空"误记成"用户原来的壁纸"，这条坏记录会被自动清掉。
- 配置文件改为原子写入，断电/被 kill 不会再留下半份 JSON 把设置全冲掉。
- 自动检查更新失败时不再记时间戳，断网一次不会导致一整天不再重试。
- 托盘图标在找不到图标文件时（比如直接跑仓库的开发模式）会用程序画一个兜底，
  绝不给出空像素图——空像素图会让 AppIndicator 扩展抛异常，连累所有托盘图标。
- 托盘项补齐了扩展会主动索取的几个附属属性，扩展不会再因为我们而报未捕获异常。
- 「生成离线动态壁纸」会先清掉上一次的帧文件（几十 MB），不再新旧混在一起。

**菜单收拾了一遍**

- 顶层从十几行收到 6 行：`换一扇窗 / 跟随真实天气 / 沉浸全屏`、
  `桌面壁纸 ▸`、`看 ▸`、`开机与关窗 ▸`、`关于与帮助 ▸`、`退出`。
  不常用的开关都收进对应的子菜单，托盘里的菜单同步变化。
- 「关于窗」里补上了作者 **蓝色奇夸克**（[github.com/lsqkk](https://github.com/lsqkk)）的主页链接。
- 新增 **问题反馈 / 提个建议（GitHub）**：一键打开仓库的"新建 issue"页，
  并预填版本号、GTK / libadwaita 版本、会话与桌面环境、托盘与壁纸状态
  （只有这些环境信息，不含位置，打开后你还能自己删）。菜单里另有「作者的主页」。

**顺手修掉的三个旧毛病**

- 「自动检查更新」这个菜单项以前**没有注册对应动作**，点了没有任何反应（一直是灰的）。
- 窗口右上角的**图钉按钮点了不生效**：它走的是 `activate(action, Variant)`，
  而勾选项的动作参数类型是 `None`，GLib 会断言失败后静默返回（AGENTS.md 里记过的那条坑）。
  现在统一走 `set_toggle()`：先改状态再跑处理器，菜单、托盘、配置三者一致。
- 做好「离线动态壁纸」或「还原成原来的壁纸」之后，「壁纸跟随此刻」的勾还留在原处
  （配置关了、界面没关）。现在一并收敛。

## [1.1.0] — 2026-09-23

**检查更新**

- 菜单新增「检查更新」与「自动检查更新」（默认开，一天一次）。
  只向 GitHub Releases 接口询问最新版本号，不发任何身份信息，可随时关闭。
- 发现新版本时，菜单顶部会出现入口：打开发布页 / 下载 `.deb` 安装包 / 跳过这个版本。
- 命令行也能用：`chuang --version`、`chuang --check-update`
  （有新版本时退出码为 10，方便脚本判断）。
- 网络不可用时静默降级：自动检查不打扰，手动检查会明确告知失败原因。

## [1.0.0] — 2026-09-23

第一个正式版本。

**天空**

- 本地天文计算：太阳（NOAA 算法）、月亮（Meeus 第 47 章，含相位与亮面朝向）、
  约 1600 颗恒星（星等 ≤ 5.0）、完整暮光分级与升落时刻。
- 以太阳高度角为唯一驱动量的天色色板，在正午蓝与深夜黑之间连续过渡。
- 星空、月亮（含相位与亮面方向）、日出日落、金色时刻、月出月落。

**天气**

- 接入 Open-Meteo，把真实的云量、云型、雨、雪、雷、雾画进窗里；断网自动退回纯天文模式。
- 厚度不同的三层云、随风向倾斜的雨丝、飘落的雪、闪电与城市光晕。

**物件**

- 窗台：受光、光斑，以及一盆小植物——它的影子方向与长短由真实的太阳方位角与高度角决定。
- 地平线下的街景：行人、自行车、汽车、公交，按天气与昼夜变化（雨天撑伞、夜里开灯、
  高楼窗户渐次亮起）。位置是时间的函数，因此连续移动。

**交互**

- 「今日天色」长卷：今天 00:00–24:00 的光色曲线，可拖动做时间旅行。
- 空格查看「此刻的事实」，F11 沉浸全屏，图钉窗口置顶。
- 系统托盘（KStatusNotifierItem + DBusMenu），菜单与应用菜单同源。
- 关窗时询问"最小化到托盘 / 直接退出"并记住。

**桌面壁纸**

- 壁纸跟随此刻（每 10 秒换一张）、离线动态壁纸（GNOME 定时 XML，96 帧 / 15 分钟一帧）、
  单张快照；可把「此刻的事实」与「今日天色」一并画进壁纸；一键还原原壁纸。

**工程**

- 纯 Cairo 绘制，无位图素材；零第三方 Python 依赖（只用 PyGObject）。
- 离屏渲染 + 缓存（天空底色、云层、暗角），窗口可见时约一颗核的 10%。
- 提供 `.deb` 打包脚本、截图生成脚本与两个调试用的假装时刻/天气环境变量。
