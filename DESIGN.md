# 窗 · Chuang — 设计文档

> 一句话：**把你头顶此刻真实的天空，搬到桌面的一扇窗里。**

---

## 1. 它要解决的"场景"

它不是一个"打开干完活就关掉"的工具，而是一个会被**反复瞄一眼**的桌面物件：

| 场景 | 人的念头 | 窗的回应 |
|---|---|---|
| 在无窗房间/办公室呆久了 | 「现在外面天黑了没？」 | 一眼看到真实天色 |
| 午睡醒来 / 起床 | 「今天光怎么样？」 | 今天的光线曲线 + 此刻位置 |
| 想出门 / 拍照 | 「日落还有多久？金色时刻几点？」 | 精确倒计时 + 太阳方位角 |
| 深夜加班 | 「外面在下雨吗？」 | 窗上正在下雨（真实天气镜像） |
| 想安静一下 | 「想要一点非屏幕的东西」 | 一扇会自己变化的窗，常驻置顶当"桌面夜景灯" |

---

## 2. 创新性验证（做的功课）

在动手前，用可复核的渠道检索了"同类桌面软件是否已存在"：

| 渠道 | 检索词 | 结果 |
|---|---|---|
| Flathub 应用库 API | `sky` `sundial` `virtual window` `daylight` `night sky` `sun clock` | 无同类。命中都是**重量级星象软件**（Stellarium、Gaia Sky）或**天气预报器**（Meteo、KMeteo）、时钟挂件（Retro） |
| Arch AUR | `virtual-window` `sky` `sundial` `daylight` `window` | 无同类。只有命令行工具 `daylight`（打印日出日落），与 `askyy`（终端 ASCII 星空屏保） |
| Ubuntu/Debian 仓库 | `sun` `daylight` `solar` `moon` `sky` | 只有天气预报/系统指示器（gnome-weather、indicator-china-weather），无天空渲染类桌面应用 |
| 已装应用清单 | — | 无同类 |

**结论：**
1. 「实时真实天空渲染」这个能力，在 Linux 桌面生态里只存在于**专业天文馆软件**中，而那是"看宇宙"的工具，不是"看我窗外"的物件。
2. 「虚拟窗户」这个形态在移动端（iOS/Android）有零星的付费应用，在 **Linux 桌面端空缺**。
3. 现有"看天"的软件都停留在**数字/文字**层面（温度、日出时间），没有人把它做成**可感知的光与色**。

因此创新点不在于"算太阳位置"（这是成熟算法），而在于这个**组合与形态**：

> 真实天文计算（离线可算） + 真实天气镜像（在线则同步） + **影子与光都物理正确地落在窗台上** + 「今日天色」可回溯可预览的光色长卷 + 一扇可以置顶的小窗。

---

## 3. 设计原则（为什么它"清新、有记忆点、不臃肿"）

1. **零输入。** 打开即正确。没有账号、没有设置向导拦路：首次运行按系统时区猜一个城市，
   只弹一条提示说"猜的是这里、不对的话菜单里换"——**刻意不弹模态框逼人确认**，
   那等于拿"零输入"去换一次确认（1.1.8 的手动坐标时区修正就是为了让这个猜测更靠得住）。
2. **一个动词：看。** 全应用只有"看"这一个主要动作，其余都是它的注脚。
3. **诚实感是记忆点。** 影子朝哪、月亮缺哪一边、21:40 天该多暗——都和外面一致。用户第一次发现"它是对的"时，就记住了它。
4. **克制。** 总计 6 个功能，全部围绕"此刻的天空"。没有历史天气页、没有未来七天的
   预报页、没有社交、没有主题商店。（"能预览的那几天必须是真天气"是**诚实**这条
   的延伸，不是加了一个预报功能：数据按天取回来，但只在时间旅行时用，界面上仍然是
   一扇窗 + 一条长卷，见 §4.4。）

---

## 4. 功能范围（最终版，刻意保持很短）

1. **真实天空**：由经纬度与时刻**离线计算**太阳、月亮（含相位与亮面朝向）、
   约 1600 颗星（星等 ≤ 5.0）、完整暮光分级，绘制出自天光到夜色的连续变化。
2. **天气镜像**（可关）：有网络时，云量、云型、降水都在窗上真实发生——下雨就下雨，下雪就下雪，雷雨会闪电，雾会糊掉远处的屋顶。
3. **窗台光影**：窗台上放着一株小植物，它的**影子的方向和长短由真实太阳方位角与高度角决定**（清晨影子长、正午最短、日落后只剩天光）。
4. **今日天色**：窗底一条极细的光色长卷，是今天 00:00–24:00 的天色连续变化（本地计算 + 逐时云量），当前时刻有一个小游标。**可以用鼠标拖动"时间旅行"**，预览今晚 21:00 是什么颜色；松手后停在那一刻（顶部会有一条"回到此刻"，按 Esc / Home 也行），也可以用菜单里的「跳到某天某时…」直接选日期和时间。
   拖动预览时，云与气温取的是**那一刻**的逐小时预报——所以手上的预报要覆盖得到
   那些日子（默认 7 天，预览到更远就先按那一天问一次；真没有就直说"这天还没有预报"，
   不拿邻天顶替）。
5. **此刻的事实**（左上角那张卡，空格收起/展开）：大字时刻、日出到日落的「日弧」、
   太阳方位高度、金色时刻、月亮相位与月出月落、此刻天气，以及一句人话
   （如"太阳在你的西南方，仰角 12°"）。**这张卡是可以点的**，而且**分三层**
   （1.1.13 重排的版式，见 §4.2）：主角是此刻的天气（点了摊开完整的一份天气）、
   指标格是日月与日出日落（点了跳到那一刻去看）、小字条是湿度 / 风 / 紫外线这些
   "看一眼就好"的读数（点了同样摊开详情）；右上角收起、右下角刷新；底下那行
   写清楚**这份天气是什么时候问回来的**，断网时说"上次天气"。
   设计意图是"看一眼就懂，想深究就点开"——静止的卡片只有一屏，把"能钻下去"的
   动作留给鼠标，而不是往卡片里塞更多字；层次则是为了"一眼"：**大小与颜色就是
   优先级**，不要读者自己从一列等重的行里挑重点。
   图标一律 Cairo 画出来，不引图标字体也不引素材（依赖表不变）。
   这张卡在壁纸上可以单独设版式（跟随窗口 / 精简一条 / 完整版）：窗口收成小条时，
   桌面上的那张也跟着收。
6. **当窗用**：F11 沉浸全屏；可置顶成窄窗，放在屏幕角落当"桌面夜灯"；可设置开机自启。
7. **挂到桌面上**：两种取舍——「壁纸跟随此刻」按节奏重画并替换壁纸（默认 10 秒，
   可改成 30 秒 / 1 分钟；时间、天色、行人都跟着走，需要程序留在托盘）；「离线动态壁纸」用 GNOME
   原生定时 XML，关掉程序也有效但粒度是 15 分钟。**不做**"铺满桌面的窗口"那种
   伪动画：GNOME 的桌面图标在系统图层，客户端窗口一定压住它，会毁掉桌面的可用性。
8. **收进托盘**：关窗时问一次"最小化到托盘还是直接退出"并记住；托盘菜单就是
   应用菜单本身（同一份 Gio.Menu 翻译成 dbusmenu），两边永远一致。
9. **街上有人**：行人与车辆按天气与昼夜变化（雨天撑伞、夜里开灯、高峰车多），
   位置是时间的函数，于是动态壁纸的帧间过渡看起来是在真的走动。
   速度按真实观感分级（汽车 ~10 秒穿画面、公交 15 秒、自行车 20-26 秒、行人
   54-83 秒）——行人比车慢一个数量级，不然会像在赶车。时间旅行预览时，天色停在
   被预览的那一刻，但人车按"预览开始后真正走过的时间"继续走，画面不会变成照片。

10. **跳到某天某时**：拖长卷只能左右挪，想看三天后的日落得拖很久。菜单里的
    「跳到某天某时…」给一个日历 + 时/分选框，一步到位（拖动、滚轮、方向键照旧）。

12. **场景开关**（1.1.13）：窗外画什么可以挑——行人、车辆、行道树、路灯、飞机、
    城市天际线、云、星空、雨雪、盆栽、盆栽是否随风摇。理由有两个：一是"我只想
    看那片天"是完全合理的使用方式（也是性能最省的方式），二是**把编排权还给用户**
    比替他想更好——所以除了逐项开关，另给「全部画出来」与「只看天空」两个一键。
    开关只影响"画不画"，天文与天气本身照算（关掉城市不会让太阳的位置变假）。

11. **把更新装在自己身上**：发现新版本后，程序自己下 `.deb`，再开一个终端跑
    `sudo apt-get install`（输一次密码），装完检测到 dpkg 里的版本变了就问要不要
    重启；重启时先等旧进程退出（单实例锁还在手上）再拉新进程。凡是需要用户照抄的
    内容（路径、命令、报错）都放进一个可选中、可复制的「详情」窗，而不是只弹一句
    看不走的提示。

### 4.1 两条"看不见但很容易出错"的边界

**同一时刻只允许一扇窗。** 壁纸是"就地重写桌面正在显示的那张图"（理由见下），
一旦有两个进程同时往同一张图里写，画面就会在两次写入之间来回闪（一张是"现在"、
另一张可能是几秒前）。所以程序用进程间文件锁保证单实例：第二次启动会把已有窗口
叫到前台然后退出。同理，"该写哪一张"不能相信配置里记的槽位，而是先读**桌面此刻
真正显示的是哪一张**（GNOME 深浅色模式下生效的键不同）——写错文件等于这次换图
没发生。

**自启项会过期。** `~/.config/autostart/chuang.desktop` 里写的是"用户勾选那一刻"
的绝对路径；换了安装方式（源码安装 `/usr/local/bin/chuang` → `.deb` 的
`/usr/bin/chuang`）之后它就指向一条不存在的命令，而 GNOME 只会**静默**忽略整条
自启项。所以每次启动都核对一遍并就地修好，不要指望用户会去看日志。

**壁纸要写"正在显示的那一张"。** gnome-shell 把壁纸的解码结果按**文件**缓存
（`Meta.BackgroundImageCache`），而且只为"当前正在显示的那个文件"挂文件监听：
文件一变就 `purge` + 重读。反过来，如果写的是另一张、再把 URI 切过去，shell 会
直接从缓存里取出**这个文件上一次的解码结果**——桌面于是显示"这个文件名上一版/上几版"
的画面（时间也就是旧的，而且能不能刷全看两条通知谁先到，所以"有时候"）。
所以：常态**就地更新正在显示的那个文件**；只有接管（桌面挂着别人的图）与每 5 分钟
兜底一次才真的换文件名，并且换完立刻 `touch` 两下 mtime，逼 shell 丢掉旧解码。
（1.1.4 之前这个逻辑是反的，README/AGENTS 里的旧结论也已更正。）

**时间的天真之处：夏令时那两天。** 「今日天色」长卷按"本地 00:00 → 24:00、
每 5 分钟一格"生成，动态壁纸按"一天 / 96 帧"切，两条时间轴都当"一天有整整 24 小时"。
夏令时切换的那两天，真实的一天是 23 或 25 小时，所以那两天长卷与真实时间会错开
一小时（仅那一天，且只在会切夏令时的地区）。当前主要用户在中国，暂不处理——
但知道这件事，比将来对着"怎么差一小时"发懵要好。

**线程模型。** 有五类后台线程：天气抓取、壁纸单帧渲染、动态壁纸整日渲染、
城市搜索（含按时区反查）、更新检查与下载。它们的共同约定是：**线程里只做纯计算与
网络，回主线程一律 `GLib.idle_add`**；共享状态（画笔、引擎、缓存）由各自的锁或
"一次只跑一个"（`Worker._busy`）兜住。所以在窗口里改任何东西都不要绕过这条线——
GTK 对象只能在主线程碰。

**刻意不做**：七天预报、气象地图、闹钟提醒、多城市并排、桌面壁纸替换、星空科普百科、星座连线（会显得像教学软件）。

### 4.2 信息卡的三层，和"影子朝哪边"

**信息卡为什么分三层**（1.1.13）。以前的六行是等重的：图标、标题、数值、副值，
行高也一样——"此刻在下多大的雨"和"太阳的仰角"看起来一样重要，而且是竖着一长溜，
一张卡占掉半个窗口。改版的原则只有一句：**大小与颜色就是优先级**。

1. **主角**（最大）：此刻的天气。大字温度 + 一句话天气，右边一行小字是体感、
   云量、今日高低温。没有天气时，这一块不摆假度数，而是直接说为什么没有
   （"未联网 · 仅天文模式" / "这天还没有预报"）。
2. **指标格**（中等，两列）：太阳、日出、日落、月亮。它们的共同点是"点了能跳到
   那一刻"，所以长得一样、排成两列——比一列省一半高度。
3. **小字条**（最小最淡）：湿度、风、降水、能见度、紫外线、气压、露点。
   按文字宽度贪心折行（不等宽三列会把"风 西南 12.4 km/h"挤成两截）。

三者都能点：主角与小字条摊开完整数据，指标格跳到那一刻。加一行新的事实只要给它
一个 `action`，它自己就会落到该去的那一层（`render._info_groups`）。

**层次不只靠"分组"，还靠字号与基线。** 三层用三档字号（主角 27 磅的温度、
指标格 14 磅的数值、小字条 11 磅的读数），同一格里再分三档（标题 11 / 数值 14 /
副值 9.5）——"仰角 54.6°"必须明显小于"太阳"，因为它是这条事实的注脚而不是标题。
所有文字**按基线**排（`render.draw_text_bl`）：把不同字号的字按"文本框左上角"
对齐，基线会差出好几像素，看上去就是"有的偏上、有的偏下"——1.1.13 的第一版
就是这么被一眼看出来的。

**"影子朝哪边"是这个项目的诚实底线之一**，所以这里写清楚推导：光源是**窗外远处
的太阳**，于是窗台上的盆栽，它的影子落在窗台这个**水平面**上、朝观察者这一侧
（屏幕下方）拉长，横向再往背光侧偏——太阳在东，影子偏西；正午太阳在正对面，
影子几乎正对着落下来。长度按 `株高 / tan(太阳高度角)`。屏幕上的两个修正：

* **纵向要压扁**：窗台在画面里只有百来像素深（透视把纵深压得很扁），横向却是
  一比一。所以纵向按透视系数收着画。
* **低太阳下要收着画**：真按物理比例，17:30 那会儿的影子会长到横穿整扇窗。
  等比收进"半个株宽、大半个株高"，并且**越长越淡**——这是艺术化处理，
  和"日月大小放大"一样，是为了让物理正确的方向仍然看得见，而不是反过来编一个方向。

影子画在 1/4 分辨率的离屏图上再放大回原尺寸：Cairo 没有模糊，而"缩小再双线性
放大"就是一次廉价的高斯模糊。植物叶子之间全是缝，硬边剪影会看着像水里的倒影。
（`tests/test_scene_options.py` 把这条钉住了：正午影子必须朝下、上午偏西、傍晚偏东、
整条影子必须留在窗台里，而且真的画一帧看盆底之下有没有变暗。）

---

## 5. 视觉设计

- **主视觉**：天空占画面 ~72%，地平线下方是剪影（远山 + 城市屋顶，程序生成、按城市名做种子，所以每个城市长得不一样，且稳定不变）。
- **地平线上下是一整套光照模型**（1.1.5 起）：城市拆成远山 / 远景楼群 / 近景建筑三层，
  楼形按城市名生成（塔楼、写字楼、带阳台的住宅、坡屋顶小楼、带雨棚的店面、锯齿顶厂房，
  外加屋顶水箱、楼梯间、烟囱、天线）。每栋楼有自己的固有色，受光面、背光面、
  彼此之间的遮挡、楼根的环境光遮蔽、窗格（白天映天、夜里亮灯）都由"太阳方位角 ×
  高度角 × 直射光 × 环境光"算出来——太阳偏左，左侧立面就亮、影子就往右倒；
  云厚、下雨、起雾时直射光被削掉，影子跟着淡到没有。街上的行人、车辆、
  以及窗台上那盆植物用的是同一个光源，所以同一时刻画面上所有影子都指向同一边。
- **窗户感**：极细的窗框内阴影 + 窗台。窗台有一束**按真实太阳方位落下的光斑**，光斑在一天里横移。
- **街上的人与车是"有人住的城市"而不是装饰**（1.1.6 起）：路边有行道树（站在远侧
  人行道，按透视比人矮一档）；**路上有几辆车、几个人由时刻 × 星期 × 天气决定**——
  工作日早晚高峰最挤、周末平缓、凌晨几乎空街，雨天行人少、骑车几乎没有。数量是按
  "每一趟均匀挑几辆"轮换的，换班发生在车正好绕回画面外的时刻，所以看不见谁凭空
  出现；同一条道上的车速度一致，不会叠在一起。
- **配色**：以太阳高度角为唯一驱动量的色板（天顶色/地平色/太阳辉光/环境光强），共 10 个关键帧线性插值，从正午的 #1f5fae→#cfe3f5，到日落的橙金，到深夜的 #050a1a。
- **动效**：云随风向漂移、雨丝有风向倾角、星空有细微闪烁、月亮有光晕、飞过的夜航飞机有闪烁航灯。全部按真实参数驱动，不是随机播放。
- **雨雪按真实雨量画**（1.1.12 起）：雨丝的条数、长度、速度、透明度、有没有那层
  "雨幕"，全部由 Open-Meteo 的降水量（mm/时）算出来——毛毛雨是飘着几丝、大雨才是
  满窗的长划子（数值过一遍对数刻度，因为雨量本身跨三个数量级）；毛毛雨那一族封顶，
  接口给的数再离谱也不画成倾盆。雨落在**窗外**：窗框、玻璃反光和窗台上的盆栽挡在
  它前面，雨点砸在湿马路上会溅开一圈圈小涟漪。卡片、长卷悬停、详情里说的"多大"
  也按同一个数来，不和代码表上的说法打架。
- **桌面壁纸要给底部面板让路**：壁纸是铺满整屏的，而常驻的 dock 压在最下面
  （本机 64 像素）。长卷与它下面那行整点标签按**真实的 workarea** 往上让，
  读不到 workarea 就退回按像素估的余量（见 `wallpaper.screen_bottom_inset`）。
- **帧率是个取舍，所以交给用户**（1.1.9 起）：重绘由 GTK 帧时钟驱动，
  默认 60 帧/秒，菜单里可以落到 24 / 30 / 45 / 60 / 120。一帧的绘制成本
  本机实测约 10 ms（960×620）到 24 ms（1920×1080 雨天），所以帧率与 CPU
  基本是线性的：60 帧约半颗核，30 帧约四分之一。窗口拿不到焦点时按最多 30 帧
  跑省电，并且**明说这件事**（按钮提示、CHANGELOG、README 都写了），
  不做"看起来选了 120 其实在跑 30"的沉默降级。
- **文字**：只在需要时才出现。信息面板做成半透明玻璃卡片，其余时间画面里没有一个字。
- **技术**：GTK4 + libadwaita，天空由 Cairo 逐帧绘制（渐变、径向辉光、噪声云层），无位图素材，任意窗口大小都清晰。

---

## 6. 数据与隐私

- 天文计算：**纯本地数学**，无网络也能给出完全正确的天空。
- 天气：使用 Open-Meteo 免费接口（无需 API Key），只发送经纬度，不含任何身份信息；结果缓存在本地，断网时退回"纯天文模式"。
- 位置：首次由用户确认或由系统时区推测，仅存于 `~/.config/chuang/config.json`。
- 全部数据留在本机，无遥测、无账号。

---

## 7. 安装方式

- 源码：本目录（Python 3 + PyGObject，无第三方 Python 依赖）。
- 安装：`./install.sh` → 安装到 `/opt/chuang`，注册菜单项 `窗`、图标与命令 `chuang`。
- 依赖：系统已有的 `python3-gi`、`gir1.2-gtk-4.0`、`gir1.2-adw-1`、`python3-cairo`、`python3-gi-cairo`（安装脚本会自动补齐）。
